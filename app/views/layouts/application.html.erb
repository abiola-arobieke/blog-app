<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="txt-center m-100 middle">
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
      <%= link_to("logout", destroy_user_session_path, method: :delete, class: "btn") if current_user.present? %>
      <%= link_to "get your token", api_token_user_path(current_user), class: "btn" if current_user.present? %>
    <%= yield %>
    </div>
  </body>
</html>
